package com.geeklib.ether.common.utils;

import java.io.File;

// import org.eclipse.jgit.api.Git;
// import org.eclipse.jgit.transport.UsernamePasswordCredentialsProvider;

public class GitUtils {
    
    // public static void clone(String repo, String branch, String username, String password, File directory){
    //     UsernamePasswordCredentialsProvider provider = new UsernamePasswordCredentialsProvider(username, password);
    //     Git.cloneRepository()
    //         .setURI(repo)
    //         .setCredentialsProvider(provider)
    //         .setDirectory(directory);
    // }

    // public static void clone(String repo, String branch){
    //     clone(repo, branch, repo, branch, null);
    // }
}
